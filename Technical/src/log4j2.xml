<?xml version="1.0" encoding="UTF-8"?>
<!--***************************************************************************************************************

LOG4J2 CONFIGURATION FILE
V1.0.0
created by Fabian Salzgeber

*******************************************************************************************************************

LOGGING LEVEL RULES:

debug levels
TRACE   The TRACE Level designates finer-grained informational events than the DEBUG
DEBUG   The DEBUG Level designates fine-grained informational events that are most useful to debug an sample.

information levels
INFO    The INFO level is associated with significant events in the normal life cycle of the sample.

error levels
WARN    The WARN level designates potentially harmful situations.
        Example: Missing or inconsistent input parameters supplied by the user.
ERROR   The ERROR level designates error events that might still allow the sample to continue running.
        Example:  Events which usually follows a Java exception
FATAL   The FATAL level designates very severe error events that will presumably lead the sample to abort.

others
ALL     The ALL Level has the lowest possible rank and is intended to turn on all logging.
OFF     The OFF Level has the highest possible rank and is intended to turn off logging.

******************************************************************************************************************-->

<Configuration package="at.oculus.teamf.technical" status="WARN">
    <Properties>
        <Property name="log-path">logs</Property>
    </Properties>

    <Appenders>
        <!-- CONSOLE OUTPUT CONFIG -->
        <Console name="Console" target="SYSTEM_OUT">
        <PatternLayout pattern="[TIME:%d{HH:mm:ss.SSS}] [CLASS:%class{1}][METHOD:%t] %-5level -> %msg%n"/>
   </Console>

   <!-- FILE OUTPUT CONFIG -->
        <RollingFile name="RollingFile" fileName="${log-path}/${date:yyyy-MM-dd}/oculus_log-${date:yyyy-MM-dd_HH-mm-ss}.txt" filePattern="${log-path}/${date:yyyy-MM-dd_HH-mm-ss}/oculus_log-%d{yyyy-MM-dd}-%i.txt" >
            <PatternLayout pattern="[TIME:%d{HH:mm:ss.SSS}] [CLASS:%class{1}][METHOD:%t] %-5level -> %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
                <SizeBasedTriggeringPolicy size="1 MB" />
            </Policies>
            <DefaultRolloverStrategy max="20"/>
        </RollingFile>
    </Appenders>

    <!-- DATABASE OUTPUT CONFIG
        toDO: special pattern for database output
    -->


    <!-- USED LOGGERS -->
    <Loggers>
        <Logger name="OculusLogger" level="trace">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

        <Root level="trace">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>
    </Loggers>
</Configuration>


